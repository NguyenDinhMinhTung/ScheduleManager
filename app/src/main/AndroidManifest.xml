<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.system.schedulemanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PopupColorActivity"
            android:theme="@style/popupActivity" />
        <activity android:name=".TimeTableManagerActivity" />
        <activity android:name=".AddTimeTableActivity" />
        <activity android:name=".ObjectActivity" />
        <activity android:name=".AddObjectActivity" />
        <activity android:name=".ObjectDetail"
            android:theme="@style/popupActivity"></activity>
        <activity android:name=".AddEvenActivityTest"/>
    </application>

</manifest>